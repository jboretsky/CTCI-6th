#include <iostream>
#include <string>
#include <unordered_set>
#include <vector>
#include <algorithm>

using namespace std;

// have already done this on leetcode, however
// not a bad idea to try this one again here
vector<vector<int>> bomberman( vector<vector<int>> matrix ) {
}

int main() {
   vector<vector<int>> matrix{ {1,2,3},{4,5,6},{7,8,9} };
   return 0;
}
