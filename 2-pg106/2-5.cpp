#include "ll.h"

void sumLists( Node* head1, Node* head2 ) {
   // already done this on leetcode
   return;
}

int main() {
   SinglyLinkedList* list1 = new SinglyLinkedList();
   SinglyLinkedList* list2 = new SinglyLinkedList();

   list->printList();
   sumLists( list1->head, list2->head );
   list->printList();

   return 0;
}
